#ifndef H_UI
#define H_UI

// Prints the menu
void printMenu();

// Gives info about the stack
void show();

// Wait for user to press enter
void wait();

// Process user input
void processInput(int res);

#endif
